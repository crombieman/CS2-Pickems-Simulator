# AI working artifacts + docs — local only
memory/
.claude/
docs/
MEMORY.md
tasks/

__pycache__/
*.pyc

# living scratch output of optimize.py (frozen locks live in stage3_probs*.json)
data/stage3_probs_live.json

# derived D1 substrate — rebuild with `python src/bo3gg_parse.py --rebuild`
data/bo3gg/parsed.sqlite

# W6 harness derived outputs (runs, fit cache); configs stay committed
data/harness/*
!data/harness/configs/
